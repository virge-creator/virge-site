---
import Layout from '../../layouts/Layout.astro';
import CTA from '../../components/CTA.astro';
import { t } from '../../i18n';

const lang = 'en';
const i = t(lang);
---
<Layout title="Home" lang={lang}>
  <div class="hero">
    <h1><Fragment set:html={i.hero.title} /></h1>
    <p>{i.hero.description}</p>
    <a href={`/${lang}/contact/`} class="btn">{i.hero.cta}</a>
  </div>

  <section>
    <div class="container">
      <h2>{i.home.whatWeDo}</h2>
      <div class="cards">
        <a href={`/${lang}/ai/`} class="card" style="text-decoration:none;color:inherit">
          <div class="icon">🤖</div>
          <h3>{i.home.aiTitle}</h3>
          <p>{i.home.aiDesc}</p>
        </a>
        <a href={`/${lang}/sdn/`} class="card" style="text-decoration:none;color:inherit">
          <div class="icon">🌐</div>
          <h3>{i.home.sdnTitle}</h3>
          <p>{i.home.sdnDesc}</p>
        </a>
        <a href={`/${lang}/ecommerce/`} class="card" style="text-decoration:none;color:inherit">
          <div class="icon">🛒</div>
          <h3>{i.home.ecomTitle}</h3>
          <p>{i.home.ecomDesc}</p>
        </a>
        <a href={`/${lang}/openclaw/`} class="card" style="text-decoration:none;color:inherit">
          <div class="icon">🐾</div>
          <h3>{i.home.openclawTitle}</h3>
          <p>{i.home.openclawDesc}</p>
        </a>
      </div>
    </div>
  </section>

  <div class="container">
    <CTA lang={lang} />
  </div>
</Layout>
